{
    "dependencies": {
        "@matter/main": "0.15.6",
        "pigpio": "^3.3.1"
    },
    "devDependencies": {
        "@types/node": "^24.1.0",
        "typescript": "~5.8.3"
    },
    "name": "garage-manage",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "prepare": "tsc",
        "build": "tsc",
        "app": "node --enable-source-maps dist/GarageManage.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "node -e 'fs.rmSync(\"dist\", { recursive: true, force: true })'"
    },
    "description": "Garage Shutter & Light, Fan Controll Device."
}
